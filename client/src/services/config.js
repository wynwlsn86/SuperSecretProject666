// configure aws keys and buckets here

// Use variables here to store your config keys from your env file

const config = {
	bucketName: '',
	region: '',
	dirName: '',
	accessKeyId: '',
	secretAccessKey: ''
}

export default config
